{
  "name": "audio-os",
  "private": true,
  "type": "module",

  "workspaces": [
    "apps/*",
    "libs/*"
  ],

  "scripts": {
    "dev": "echo \"run app dev script\"",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf node_modules **/node_modules"
  },

  "devDependencies": {
    "typescript": "^5.4.0",
    "rimraf": "^5.0.0"
  }
}